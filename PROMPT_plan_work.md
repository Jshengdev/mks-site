Study specs/README.md (the Pin) and @IMPLEMENTATION_PLAN.md (if present).

SCOPED PLANNING for: "${WORK_SCOPE}"

Study existing code and compare against specs. Plan ONLY tasks relevant to the scope above.
If uncertain whether a task belongs to this scope, exclude it.
For each task, cite the spec and source files. Derive required tests from acceptance criteria.

Plan only. Do NOT implement. Confirm with search before assuming anything is missing.
